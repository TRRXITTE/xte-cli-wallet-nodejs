import { Observable } from '../Observable';
import { buffer as higherOrder } from '../operators/buffer';
this;
Observable < T > , closingNotifier;
Observable();
Observable < T[] > {
    return: higherOrder(closingNotifier)(this), as: Observable()
};
