import { Observable } from '../Observable';
import { bufferCount as higherOrder } from '../operators/bufferCount';
this;
Observable < T > , bufferSize;
number, startBufferEvery;
number = null;
Observable < T[] > {
    return: higherOrder(bufferSize, startBufferEvery)(this), as: Observable()
};
