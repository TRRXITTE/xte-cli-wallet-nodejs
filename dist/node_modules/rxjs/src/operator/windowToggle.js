import { Observable } from '../Observable';
import { windowToggle as higherOrder } from '../operators/windowToggle';
this;
Observable < T > , openings;
Observable < O > ,
    closingSelector;
((openValue) => Observable());
Observable < Observable < T >> {
    return: higherOrder(openings, closingSelector)(this), as: Observable()
};
