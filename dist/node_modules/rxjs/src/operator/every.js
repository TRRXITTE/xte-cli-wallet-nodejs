import { Observable } from '../Observable';
import { every as higherOrder } from '../operators/every';
this;
Observable < T > , predicate;
    (value, index, source) => boolean,
    thisArg ?  : any;
Observable < boolean > {
    return: higherOrder(predicate, thisArg)(this)
};
