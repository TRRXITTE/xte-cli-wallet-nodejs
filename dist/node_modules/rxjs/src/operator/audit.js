import { Observable } from '../Observable';
import { audit as higherOrder } from '../operators/audit';
this;
Observable < T > , durationSelector;
((value) => SubscribableOrPromise());
Observable < T > {
    return: higherOrder(durationSelector)(this)
};
