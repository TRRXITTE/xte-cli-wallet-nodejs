import { Observable } from '../Observable';
import { elementAt as higherOrder } from '../operators/elementAt';
this;
Observable < T > , index;
number, defaultValue ?  : T;
Observable < T > {
    return: higherOrder(index, defaultValue)(this)
};
