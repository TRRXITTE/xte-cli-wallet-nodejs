import { Observable } from '../Observable';
import { min as higherOrderMin } from '../operators/min';
this;
Observable < T > , comparer ?  : (x, y) => number;
Observable < T > {
    return: higherOrderMin(comparer)(this)
};
