import { Observable } from '../Observable';
import { distinctUntilKeyChanged as higherOrder } from '../operators/distinctUntilKeyChanged';
this;
Observable < T > , key;
string;
Observable();
this;
Observable < T > , key;
string, compare;
((x, y) => boolean);
Observable();
this;
Observable < T > , key;
string, compare ?  : (x, y) => boolean;
Observable < T > {
    return: higherOrder(key, compare)(this)
};
