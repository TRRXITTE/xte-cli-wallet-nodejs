import { Observable } from '../Observable';
this;
Observable < T > , predicate;
    (value, index) => boolean, thisArg ?  : any;
[Observable < T > , Observable()];
{
    return higherOrder(predicate, thisArg)(this);
}
