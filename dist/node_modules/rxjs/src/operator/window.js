import { Observable } from '../Observable';
import { window as higherOrder } from '../operators/window';
this;
Observable < T > , windowBoundaries;
Observable();
Observable < Observable < T >> {
    return: higherOrder(windowBoundaries)(this), as: Observable()
};
