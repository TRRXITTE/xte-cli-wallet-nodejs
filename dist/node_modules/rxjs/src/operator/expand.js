import { Observable } from '../Observable';
import { expand as higherOrder } from '../operators/expand';
this;
Observable < T > , project;
    (value, index) => Observable < T > , concurrent ?  : number, scheduler ?  : IScheduler;
Observable();
this;
Observable < T > , project;
    (value, index) => Observable < R > , concurrent ?  : number, scheduler ?  : IScheduler;
Observable();
this;
Observable < T > , project;
    (value, index) => Observable < R > ,
    concurrent;
number = Number.POSITIVE_INFINITY,
    scheduler;
IScheduler = undefined;
Observable < R > {
    concurrent:  = (concurrent || 0) < 1 ? Number.POSITIVE_INFINITY : concurrent,
    return: higherOrder(project, concurrent, scheduler)(this)
};
