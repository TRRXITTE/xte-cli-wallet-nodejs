import { Observable } from '../Observable';
import { bufferWhen as higherOrder } from '../operators/bufferWhen';
this;
Observable < T > , closingSelector;
(() => Observable());
Observable < T[] > {
    return: higherOrder(closingSelector)(this), as: Observable()
};
