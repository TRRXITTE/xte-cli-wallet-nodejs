import { Observable } from '../Observable';
import { shareReplay as higherOrder } from '../operators/shareReplay';
this;
Observable < T > , bufferSize ?  : number, windowTime ?  : number, scheduler ?  : IScheduler;
Observable < T > {
    return: higherOrder(bufferSize, windowTime, scheduler)(this), as: Observable()
};
