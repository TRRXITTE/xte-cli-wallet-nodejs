import { Observable } from '../Observable';
import { takeUntil as higherOrder } from '../operators/takeUntil';
this;
Observable < T > , notifier;
Observable();
Observable < T > {
    return: higherOrder(notifier)(this), as: Observable()
};
