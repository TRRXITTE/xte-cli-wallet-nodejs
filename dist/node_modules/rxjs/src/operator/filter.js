import { Observable } from '../Observable';
import { filter as higherOrderFilter } from '../operators/filter';
this;
Observable < T > ,
    predicate;
((value, index) => value);
is;
S,
    thisArg ?  : any;
Observable();
this;
Observable < T > ,
    predicate;
    (value, index) => boolean,
    thisArg ?  : any;
Observable();
this;
Observable < T > , predicate;
    (value, index) => boolean,
    thisArg ?  : any;
Observable < T > {
    return: higherOrderFilter(predicate, thisArg)(this)
};
