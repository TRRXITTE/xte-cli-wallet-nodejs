import { Observable } from '../Observable';
import { distinctUntilChanged as higherOrder } from '../operators/distinctUntilChanged';
this;
Observable < T > , compare ?  : (x, y) => boolean;
Observable();
this;
Observable < T > , compare;
    (x, y) => boolean, keySelector;
((x) => K);
Observable();
this;
Observable < T > , compare ?  : (x, y) => boolean, keySelector ?  : (x) => K;
Observable < T > {
    return: higherOrder(compare, keySelector)(this)
};
