import { Observable } from '../Observable';
import { last as higherOrder } from '../operators/last';
this;
Observable < T > ,
    predicate;
((value, index, source) => value);
is;
S;
Observable();
this;
Observable < T > ,
    predicate;
((value, index, source) => value);
is;
S,
    resultSelector;
    (value, index) => R, defaultValue ?  : R;
Observable();
this;
Observable < T > ,
    predicate;
((value, index, source) => value);
is;
S,
    resultSelector;
void ,
    defaultValue ?  : S;
Observable();
this;
Observable < T > ,
    predicate ?  : (value, index, source) => boolean;
Observable();
this;
Observable < T > ,
    predicate;
    (value, index, source) => boolean,
    resultSelector ?  : (value, index) => R,
    defaultValue ?  : R;
Observable();
this;
Observable < T > ,
    predicate;
    (value, index, source) => boolean,
    resultSelector;
void ,
    defaultValue ?  : T;
Observable();
this;
Observable < T > , predicate ?  : (value, index, source) => boolean,
    resultSelector ?  : ((value, index) => R) | void ,
    defaultValue ?  : R;
Observable < T | R > {
    return: higherOrder(predicate, resultSelector, as, any, defaultValue)(this)
};
