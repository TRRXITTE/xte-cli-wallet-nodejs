import { Observable } from '../Observable';
import { switchMap as higherOrderSwitchMap } from '../operators/switchMap';
this;
Observable < T > , project;
((value, index) => ObservableInput());
Observable();
this;
Observable < T > , project;
    (value, index) => ObservableInput < I > , resultSelector;
((outerValue, innerValue, outerIndex, innerIndex) => R);
Observable();
this;
Observable < T > , project;
    (value, index) => ObservableInput < I > ,
    resultSelector ?  : (outerValue, innerValue, outerIndex, innerIndex) => R;
Observable < I | R > {
    return: higherOrderSwitchMap(project, resultSelector)(this)
};
