import { Observable } from '../Observable';
import { zipAll as higherOrder } from '../operators/zipAll';
this;
Observable < T > , project ?  : (...values) => R;
Observable < R > {
    return: higherOrder(project)(this)
};
