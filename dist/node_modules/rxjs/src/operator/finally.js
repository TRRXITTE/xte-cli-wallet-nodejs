import { Observable } from '../Observable';
import { finalize } from '../operators/finalize';
this;
Observable < T > , callback;
(() => void );
Observable < T > {
    return: finalize(callback)(this), as: Observable()
};
