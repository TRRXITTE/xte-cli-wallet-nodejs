import { Observable } from '../Observable';
import { sequenceEqual as higherOrder } from '../operators/sequenceEqual';
this;
Observable < T > , compareTo;
Observable < T > ,
    comparor ?  : (a, b) => boolean;
Observable < boolean > {
    return: higherOrder(compareTo, comparor)(this)
};
