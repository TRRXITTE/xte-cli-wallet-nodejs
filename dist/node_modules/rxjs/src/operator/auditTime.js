import { async } from '../scheduler/async';
import { Observable } from '../Observable';
import { auditTime as higherOrder } from '../operators/auditTime';
this;
Observable < T > , duration;
number, scheduler;
IScheduler = async;
Observable < T > {
    return: higherOrder(duration, scheduler)(this), as: Observable()
};
