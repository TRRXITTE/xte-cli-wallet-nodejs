import { Observable } from '../Observable';
import { mergeScan as higherOrder } from '../operators/mergeScan';
this;
Observable < T > ,
    accumulator;
    (acc, value) => Observable < R > ,
    seed;
R,
    concurrent;
number = Number.POSITIVE_INFINITY;
Observable < R > {
    return: higherOrder(accumulator, seed, concurrent)(this)
};
