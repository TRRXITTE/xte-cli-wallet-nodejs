import { Observable } from '../Observable';
import { subscribeOn as higherOrder } from '../operators/subscribeOn';
this;
Observable < T > , scheduler;
IScheduler, delay;
number = 0;
Observable < T > {
    return: higherOrder(scheduler, delay)(this), as: Observable()
};
