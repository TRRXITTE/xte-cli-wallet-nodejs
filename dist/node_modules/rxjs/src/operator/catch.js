import { Observable } from '../Observable';
import { catchError as higherOrder } from '../operators/catchError';
this;
Observable < T > , selector;
((err, caught) => ObservableInput());
Observable < T | R > {
    return: higherOrder(selector)(this)
};
