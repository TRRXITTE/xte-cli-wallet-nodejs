import { Observable } from '../Observable';
import { mergeMapTo as higherOrder } from '../operators/mergeMapTo';
this;
Observable < T > , observable;
ObservableInput < R > , concurrent ?  : number;
Observable();
this;
Observable < T > , observable;
ObservableInput < I > , resultSelector;
    (outerValue, innerValue, outerIndex, innerIndex) => R, concurrent ?  : number;
Observable();
this;
Observable < T > , innerObservable;
Observable < I > ,
    resultSelector ?  : ((outerValue, innerValue, outerIndex, innerIndex) => R) | number,
    concurrent;
number = Number.POSITIVE_INFINITY;
Observable < R > {
    return: higherOrder(innerObservable, resultSelector, as, any, concurrent)(this), as: Observable()
};
