import { map as higherOrderMap } from '../operators/map';
import { Observable } from '../Observable';
this;
Observable < T > , project;
    (value, index) => R, thisArg ?  : any;
Observable < R > {
    return: higherOrderMap(project, thisArg)(this)
};
