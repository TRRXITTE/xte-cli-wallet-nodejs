import { Observable } from '../Observable';
import { async } from '../scheduler/async';
import { defaultThrottleConfig } from '../operators/throttle';
import { throttleTime as higherOrder } from '../operators/throttleTime';
this;
Observable < T > ,
    duration;
number,
    scheduler;
IScheduler = async,
    config;
ThrottleConfig = defaultThrottleConfig;
Observable < T > {
    return: higherOrder(duration, scheduler, config)(this), as: Observable()
};
