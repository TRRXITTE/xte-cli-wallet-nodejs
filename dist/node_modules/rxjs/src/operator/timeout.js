import { async } from '../scheduler/async';
import { Observable } from '../Observable';
import { timeout as higherOrder } from '../operators/timeout';
this;
Observable < T > ,
    due;
number | Date,
    scheduler;
IScheduler = async;
Observable < T > {
    return: higherOrder(due, scheduler)(this), as: Observable()
};
