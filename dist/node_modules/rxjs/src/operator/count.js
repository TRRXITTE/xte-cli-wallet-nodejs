import { Observable } from '../Observable';
import { count as higherOrder } from '../operators/count';
this;
Observable < T > , predicate ?  : (value, index, source) => boolean;
Observable < number > {
    return: higherOrder(predicate)(this)
};
